Abordagem Dinâmica - Pilha

Remoção por RRN:

Remove (int RRN){
	--abrir o arquivo para escrita
	if (RRN < prox_RRN){
		--posiciona fseek no byte offset (RRN*tamanho do registro)
		--lê o registro
		if (removido(RRN) == true){
			printf("Registro não econtrado!");
		}
		--remove logicamente (marca como removido)
		--escreve o valor do topo no "prox" do campo
		--escreve o valor do RRN no topo
	} else {
		printf("Registro não econtrado!");
	}
	--fechar arquivo
}

Inserção:

InsertInto (){
	--abrir o arquivo para escrita
	--ler o valor do prox_RRN do registro de cabeçalho (variável global)
	--ler o valor do topo
	if (topo != -1){
		--byte offset = topo * tamanho do registro
		--posiciona a posição corrente no byte offset
		--escreve o valor do prox no topo (topo = prox_pilha)
		--escrever/inserir o registro
	} else {
		--byte offset = tam_reg + prox_RRN
		--posiciona a posição corrente no byte offset
		--escrever/inserir o registro
		--incrementar o prox RRN
	}
	--fechar arquivo
}

